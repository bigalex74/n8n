# Шаблон SKILL.md (полный — для сложных предметных областей)

```markdown
---
name: <skill-name>
description: <Когда использовать. Перечисли ключевые слова: технологии, задачи, контекст>.
---

# <Название скилла>

<Описание предметной области, 3-5 строк. Что это? Зачем? Какой масштаб?>

---

## Окружение

| Параметр | Значение |
|----------|----------|
| Путь к проекту | `/home/user/<name>` |
| Стек | Python 3.11, FastAPI, React, PostgreSQL |
| Docker | `docker compose -f docker-compose.yml` |
| Порт | `:8002` (dev), `:443` (prod через Caddy) |
| Домен | `example.bigalexn8n.ru` |
| Версия | v1.33.0 |

---

## Архитектура

```
<ASCII или текстовое описание архитектуры>
```

---

## Ключевые компоненты

### 1. <Компонент A>
- **Расположение:** `src/...`
- **Назначение:** ...
- **Важные файлы:** `main.py`, `schemas.py`
- **Паттерны:** ...

### 2. <Компонент B>
...

---

## Правила разработки

1. **Правило 1** — объяснение
2. **Правило 2** — объяснение
3. **Правило 3** — объяснение

---

## Типичные задачи и команды

| Задача | Команда |
|--------|---------|
| Деплой | `make deploy` |
| Тесты Python | `PYTHONPATH=src python -m unittest discover -s tests -q` |
| Тесты TS | `cd ui && npm test` |
| Coverage | `python -m coverage run -m unittest ... && python -m coverage report` |
| Логи контейнера | `docker logs <container> -f --tail=50` |
| Войти в контейнер | `docker exec -it <container> bash` |

---

## База данных

- **Хост:** localhost / Docker сеть
- **БД:** `postgres`, **Юзер:** `n8n_user`
- **Ключевые таблицы:**

| Таблица | Назначение |
|---------|-----------|
| `table1` | ... |
| `table2` | ... |

---

## Git Flow

- **Основная ветка:** `develop`
- **Формат веток:** `TICKET-XXX`
- **Коммиты:** Conventional Commits (`feat`, `fix`, `refactor`)
- **Push:** только в `develop`, никогда в `master` напрямую

---

## Частые проблемы и решения

### Проблема 1
**Симптом:** ...
**Решение:** ...

### Проблема 2
**Симптом:** ...
**Решение:** ...

---

## Ссылки

- [Детали настройки](references/setup.md)
- [Лучшие практики](references/best-practices.md)
- [Схема БД](references/db-schema.md)
```
