{"name": "env-doctor", "description": "Diagnose and fix .env and configuration issues.", "trigger": "env doctor", "instruction": "When user has config/.env issues:\n1. Check .env file exists and is loaded\n2. Verify all required variables are set\n3. Check for syntax errors (no spaces around =)\n4. Verify file permissions (should be 600)\n5. Check if variables are properly exported\n6. Verify no secrets committed to git", "tools": ["read_file", "run_shell_command"], "autoActivate": false}