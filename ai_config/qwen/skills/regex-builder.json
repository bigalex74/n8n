{"name": "regex-builder", "description": "Generate and test regex patterns with explanations.", "trigger": "regex", "instruction": "When user asks for regex or pattern matching:\n1. Understand the pattern requirement\n2. Generate regex with explanation\n3. Test with sample inputs\n4. Provide both strict and permissive versions", "tools": ["run_shell_command", "write_file"], "autoActivate": false}